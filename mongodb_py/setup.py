from setuptools import setup

setup(
   name='mongodb_py',
   version='1.0.0',
   description='Usar o mongo DB no python',
   author='tupy',
   author_email='tupyribas@outook.com',
   packages=['mongodb_py'],
)
